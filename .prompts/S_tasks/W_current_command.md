add a "current" command in new file in commands module
It will display the currently active task : its title and path to the prompt file. Use colors like the other commands
It will also display if Top Level or its sublevel + It should also display the number of still open siblings Like  (like when doiung pwr done or commit)
If sublevel : it should also display the hierarchy it is part of from bottom to top + if correction or prompt or just subtask (if subtask it is defaut so nothing special to display)
