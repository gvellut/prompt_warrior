Make it so it the same behaviour as done command (redirect behaviour if possible). except do not show the logging : Running done with
Add the same options:
    "-r",
    "--recursive",
to the commit command.

In the done processing (independant command or as part of commit) : if not recursive used : indicate if all the siblings are also done